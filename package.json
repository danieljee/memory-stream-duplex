{
  "name": "memory-stream-duplex",
  "version": "0.0.1",
  "description": "In-memory duplex streams (Duplex and Transform)",
  "main": "index.js",
  "scripts": {
    "build": "ls | grep -E '\\.js$' | xargs rm -f && tsc"
  },
  "author": "Daniel Jee",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^12.0.3"
  }
}
